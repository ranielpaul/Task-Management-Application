/*
  useTasks hook placeholder.

  Future contents:
  - Store the task list.
  - Store loading and error state.
  - Call TaskAPI functions.
  - Expose handlers for add, edit, delete, mark complete, search, and filter actions.
*/

export function useTasks() {
  return {
    tasks: [],
  };
}

export default useTasks;
